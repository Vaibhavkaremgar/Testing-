[phases.setup]
nixPkgs = ["python311"]
aptPkgs = ["tesseract-ocr", "libtesseract-dev", "poppler-utils"]

[phases.install]
cmds = ["pip install -r requirements.txt", "python -m spacy download en_core_web_sm"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
